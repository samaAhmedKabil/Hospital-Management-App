<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/icon_radio"
        android:src="@drawable/ic_radio_to_do"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <TextView
        android:id="@+id/text_tittle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="dasdnkasd k kas dlks "
        android:textColor="@color/black"
        android:textSize="13dp"
        android:layout_marginStart="10dp"
        app:layout_constraintBottom_toBottomOf="@id/icon_radio"
        app:layout_constraintTop_toTopOf="@+id/icon_radio"
        app:layout_constraintStart_toEndOf="@id/icon_radio"
        />

</androidx.constraintlayout.widget.ConstraintLayout>